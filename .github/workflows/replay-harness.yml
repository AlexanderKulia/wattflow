name: Correctness Replay Harness

on:
  pull_request:
  workflow_dispatch:

jobs:
  replay-harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go test ./test/correctness/...
